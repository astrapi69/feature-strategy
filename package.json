{
  "name": "feature-strategy-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Framework-agnostic component authorization and feature gating strategies",
  "license": "MIT",
  "author": "Asterios Raptis",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "typecheck": "npm run typecheck --workspaces"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
